# Generated by bin/push.sh — the digest-pinned editor the fleet pulls.
# v3 reads NVIM_IMAGE from here. The digest is the immutable truth; the tag is
# human-readable. Bump by re-running push.sh and committing this file.
# D14 EXECUTED 2026-08-16: these are manifest-list (INDEX) digests — one string
# resolves the right arch on every machine (amd64 + arm64). Never re-pin to a
# per-arch digest (docs/protocols/image-multiarch.md invariant).
NVIM_IMAGE=ghcr.io/advl/nvim@sha256:399b7b3c7df10e36cc67ebaabe2b8117927b166103322054ad71d94ffef12dc4
NVIM_BASE_IMAGE=ghcr.io/advl/nvim-base@sha256:d0d4cb8b4754fee038d06be333e96c6bd44762f2b79d761179830a0fbbcb857b
NVIM_TAG=2026.08.16
NVIM_GIT_SHA=8453c36e74f8398314cc2bbba6e20364fd710a61
NVIM_ARCHS="amd64 arm64"
